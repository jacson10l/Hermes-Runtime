project: your-project-name

current_ticket: active-ticket-name

phase: realization

active_goal:
  current implementation goal

active_constraints:
  - single ownership
  - no duplicate subscriptions
  - retry limit

active_skills:
  - websocket/reconnect
  - backend/state-machine

active_agents:
  - implementer
  - reviewer
